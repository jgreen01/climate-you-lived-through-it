{
  "name": "cs416-narrative-visualization",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling only: the site itself has no build step or runtime npm dependencies.",
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
